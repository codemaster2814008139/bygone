<%= form_with(model: era, local: true) do |form| %>
  <% if era.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(era.errors.count, "error") %> prohibited this era from being saved:</h2>

      <ul>
      <% era.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :era_title %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.text_field :time, id: :era_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
